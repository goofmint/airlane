#!/usr/bin/env node
var program = require('commander');

program
  .version('0.0.1')
  .command('init', 'Generate init code')
  .command('route [name]', 'Generate route code.')
  .parse(process.argv);
